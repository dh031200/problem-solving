#include <bits/stdc++.h>

using namespace std;

class bj_6603 {
public:
    void solution() {
        int N;
        while(true){
            cin >> N;
            if(N == 0)
                break;
            vector<int> nums(N);
            for (int i = 0; i < N; ++i) {
                cin >> nums[i];
            }
            next_permutation() //ing
        }
    }
};